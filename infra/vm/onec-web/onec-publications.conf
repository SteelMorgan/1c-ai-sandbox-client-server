# 1C web publications (managed by onec-webinst.sh).
#
# Apache reads this file from the image (enabled via a2enconf),
# and includes the mutable fragment from the persistent volume.
IncludeOptional /var/lib/onec-web/apache/onec.conf

