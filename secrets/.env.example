## Local secrets (do NOT commit). Used only by scripts/prepare-secrets.sh

# releases.1c.ru (optional if local installer exists)
ONEC_USERNAME=
ONEC_PASSWORD=

# developer.1c.ru (community activation)
DEV_LOGIN=
DEV_PASSWORD=

# GitHub (PAT) for gh CLI auth inside devcontainer (optional, but recommended)
# Used to generate secrets/github_token -> mounted as /run/secrets/github_token
GITHUB_TOKEN=

# Postgres password (used via Docker secrets)
PG_PASSWORD=

# Safety switch: allow overwriting an existing secrets/pg_password with a different PG_PASSWORD.
# Use ONLY together with resetting Postgres data volume (pgdata), otherwise Postgres will not accept the new password.
FORCE_OVERWRITE_PG_PASSWORD=0

