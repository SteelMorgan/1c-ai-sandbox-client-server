## onec-client runtime settings (NON-SECRETS ONLY).
##
## Platform version is taken from ../infra/vm/.env (ONEC_VERSION).
## Credentials are provided via files in ../secrets/ (see onec-client/README.md).

## Optional: password for VNC (noVNC web uses VNC backend).
## If empty, VNC will start without password (not recommended outside local dev).
VNC_PASSWORD=

## Host 1C server address from inside Docker
## When server runs in Hyper-V VM: set to VM LAN IP (e.g. 192.168.0.50)
HOST_1C_SERVER=host.docker.internal

