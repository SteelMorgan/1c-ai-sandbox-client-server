## Client sandbox env (NON-SECRETS ONLY).
## 1C platform version is defined in ../infra/vm/.env (server-side single source of truth).

CC_HELPER_ENABLED=1
CC_HELPER_BASE_URL=https://ai.gbig.holdings/v1

## Codex CLI — OpenAI-compatible server (non-secret config)
## Base URL is shared with Claude Code helper: CC_HELPER_BASE_URL
## Secret token is shared with Claude Code: secrets/cc_api_key -> /run/secrets/cc_api_key
CODEX_MODEL=cx/gpt-5.3-codex
CC_HELPER_VALIDATE_MODE=anthropic
CC_HELPER_MODEL=sonnet
CC_HELPER_ALIAS_OPUS=opus
CC_HELPER_ALIAS_SONNET=sonnet
CC_HELPER_ALIAS_HAIKU=haiku
CC_HELPER_API_TIMEOUT_MS=30000
CC_HELPER_DISABLE_NONESSENTIAL_TRAFFIC=1
CC_HELPER_SKIP_VALIDATE=0
CC_HELPER_SKIP_UPDATE=1

